#pragma once
#include "cate.h"

extern ptr<byte> PrintByteNumber3(ptr<byte> pVram, byte b);
extern ptr<byte> PrintByteNumber2(ptr<byte> pVram, byte b);
extern ptr<byte> PrintNumber5(ptr<byte> pVram, word w);
extern ptr<byte> PrintNumber3(ptr<byte> pVram, word w);
